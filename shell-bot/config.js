module.exports = {
    config:function(){
        return (
            {
                adminUsers:'Chat_id', //admin users' telegram id, should be numbers
                botToken: '[API_TOKEN]', // bot token

            }
        );
    }
};
